const filterFoodPrice = require('./food');

// describe("Adding function", () => {
//     test('Add 0.1 to 0.2 is 0.3', () => {
//         expect(adding(0.1, 0.2)).toBeCloseTo(0.3);
//         expect(adding(0.1, 0.2)).not.toBe(0.4);
//     });
// });

describe("Filter food price", () => {
    test('Lenght of new array is 3', () => {
        expect().x
    });
    test('New array includes kind "pepper" and price "27"', () => {
        expect().x
    });
    test('New array includes 2,3 and 4th elements from original array', () => {
        expect().x
    });
    test('Lenght of new array is 3', () => {
        expect().x
    });
    test('Lenght of new array is 3', () => {
        expect().x
    });
    test('Lenght of new array is 3', () => {
        expect().x
    });
    test('Lenght of new array is 3', () => {
        expect().x
    });

});